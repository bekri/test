function checkPassword() {
    // You don't need the password here in JavaScript anymore
    // Instead, you'll rely on the server-side authentication
    
    // Redirect to the authentication script
    window.location.href = "auth.php";
}
